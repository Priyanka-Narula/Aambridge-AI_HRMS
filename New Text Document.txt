# 1. Start Postgres + MinIO
cd D:\Aambridge-AI_HRMS
docker compose up -d

# 2. Install new dependency (once)
cd backend
..\.venv\Scripts\Activate.ps1
pip install minio==7.2.15

# 3. Ensure backend\.env has MinIO settings (already added)

# 4. Start API
uvicorn app.main:app --reload



MinIO bucket: hrms-cvs
Browse files: http://localhost:9001 (login: hrms / hrms_minio_secret)
Path inside bucket: cvs/{uuid}/{filename}.pdf